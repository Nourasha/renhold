services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: webapp_dev
    ports:
      - "5432:5432"
    volumes:
      - webapp_dev_pgdata:/var/lib/postgresql/data

volumes:
  webapp_dev_pgdata:
